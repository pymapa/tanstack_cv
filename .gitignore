# Local, per-developer Claude Code settings
.claude/settings.local.json

# Secrets / local env (already Read-denied for agents in .claude/settings.json)
.env
.env.*
!.env.example
*.local

# OS / editor noise
.DS_Store
.idea/
*.swp

# Build / deps / test output
node_modules/
dist/
dist-ssr/
.output/
.nitro/
.tanstack/
.vinxi/
.wrangler/
__unconfig*
coverage/
test-results/
playwright-report/
*.tsbuildinfo

# Local database (personal data)
data/

# Agent worktrees (feature-flow) and Playwright MCP output (page snapshots hold CV text)
.worktrees/
.playwright-mcp/
